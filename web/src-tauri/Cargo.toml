[package]
name = "zeroclaw-desktop"
version = "0.1.0"
description = "ZeroClaw Desktop"
authors = ["ZeroClaw"]
license = "MIT OR Apache-2.0"
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
strip = true

[workspace]
